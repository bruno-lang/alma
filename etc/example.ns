namespace math ::

	-use lang
	-use strict util
	
	-use shared strict vector
	-use shared foo : x y
	-use shared strict vector : a
		where 
			Int = Number
			Float = Real
			eq = equal 

	-auto [] =<> EmptyList

	-invariant Int ! even